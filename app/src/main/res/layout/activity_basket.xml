<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools" android:layout_width="match_parent"
    android:layout_height="match_parent" android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    android:paddingBottom="@dimen/activity_vertical_margin"
    tools:context=".Activity.BasketActivity">

    <ListView
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_above="@+id/btnOrder"
        android:id="@+id/android:list"
        android:padding="3dp" />

    <Button
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Order"
        android:id="@+id/btnOrder"
        android:layout_marginBottom="49dp"
        android:layout_alignParentBottom="true"
        android:layout_alignParentStart="true"
        android:elegantTextHeight="false"
        android:drawableLeft="@drawable/ic_currency_try_white_48dp"
        android:background="@color/colorPrimary"
        android:padding="3dp"
        android:onClick="order"/>

    <Button
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Remove All "
        android:id="@+id/btnCancel"
        android:onClick="cancelAll"
        android:drawableLeft="@drawable/ic_remove_shopping_cart_white_24dp"
        android:background="@color/colorPrimary"
        android:layout_alignTop="@+id/btnOrder"
        android:layout_alignParentEnd="true" />

</RelativeLayout>
